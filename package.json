{
  "name": "max",
  "version": "0.1.0",
  "description": "OpenClaw channel plugin for MAX messenger (max.ru)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "openclaw",
    "max",
    "messenger",
    "chatbot",
    "channel-plugin"
  ],
  "license": "MIT",
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ],
    "channel": {
      "id": "max",
      "label": "MAX",
      "selectionLabel": "MAX Messenger (max.ru)",
      "docsPath": "/channels/max",
      "docsLabel": "max",
      "blurb": "Мессенджер MAX — полная интеграция через MAX Bot API.",
      "order": 70,
      "aliases": [
        "max-ru"
      ]
    },
    "install": {
      "npmSpec": "openclaw-max",
      "localPath": "extensions/openclaw-max",
      "defaultChoice": "npm"
    }
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist/",
    "openclaw.plugin.json",
    "README.md"
  ],
  "peerDependencies": {
    "openclaw": ">=2026.1.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "openclaw": "*",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
